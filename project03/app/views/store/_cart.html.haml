%h1= link_to "Your Cart", cart_path
%ul#line_items 
  - cart.line_items.each do |item|
    = content_tag_for :li, item do
      = item.quantity
      ×
      = item.product.title
      —
      = number_to_currency item.total_price
%p
  Total Price:
  =number_to_currency cart.total_price
