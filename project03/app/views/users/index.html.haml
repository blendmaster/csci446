-title "Users"
%ul#users
  = content_tag_for :li, User.all do |user|
    = user.name
    = link_to "Show", user
    = link_to "Edit", edit_user_path(user)
    = link_to "Destroy", user_path(user), method: :delete
=link_to "New User", new_user_path
